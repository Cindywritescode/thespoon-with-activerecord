<h1>All Restaurants 🍽</h1>

<%= link_to '➕ add new', new_restaurant_path %>

<%# <% @restaurants.each do |restaurant| %> 
  <%# <h3><%= link_to restaurant.name, restaurant_path(restaurant) %> 
  <%# <%= '⭐️' * restaurant.rating</h3> %>
  <%# <p>Address: <%= restaurant.address %></p> 
<%# <% end %> 



<ol>
<% @restaurants.each do |restaurant| %>
  <li>
  <h3><%= link_to restaurant.name, restaurant_path(restaurant) %> <%= '⭐️' * restaurant.rating %></h3>
  </li>
<% end %>
</ol>